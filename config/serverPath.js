import { HOST } from './constants';

// 登录
export const loginUrl = `${HOST}/account/login/byPass`;

// 获取权限列表
export const getRoleListUrl = `${HOST}`;

// 获取权限列表
export const getAccoutListUrl = `${HOST}`;
